package com.feritoth.transactionprocessor.dbcore;

public class Transaction {
	
	//TODO This is the class which I would consider to be responsible for the representation of the Transaction entity from the DB
	//Here we would be declaring the annotated entity properties, the class constructors, all getters and setters + an override for the 
	//toString(), equals and hashCode methods.
	
	//In the dao sub-package we could include the interfaces and classes responsible for the DB operations on the current entity annotated as @Repository.
	//The service sub-package would be responsible for providing the business glue on the between the DB repository and the RESTful web service exposed 
	//by the application.

}