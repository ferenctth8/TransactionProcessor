package com.feritoth.transactionprocessor.utils;

public class TransactionConverter {
	
	//TODO The current class would respond for the conversion between the 2 transaction models (from the DB and JSON core part).
	//May contain one static method that is responsible for the effective conversion.
	//May be invoked by the service bean managed by Spring which communicates with the final RESTful wrapper of the appllication
	//Two more packages would be needed here for the storage of the service part and the RESTful web service respectively.

}